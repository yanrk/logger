# create some soft links for the shared object files

cd ../lib/linux

ln -s libapr-1.so.0.5.0 libapr-1.so.0
ln -s libapr-1.so.0.5.0 libapr-1.so

ln -s libexpat.so.0.5.0 libexpat.so.0
ln -s libexpat.so.0.5.0 libexpat.so

ln -s libaprutil-1.so.0.5.3 libaprutil-1.so.0
ln -s libaprutil-1.so.0.5.3 libaprutil-1.so

ln -s liblog4cxx.so.10.0.0 liblog4cxx.so.10
ln -s liblog4cxx.so.10.0.0 liblog4cxx.so

